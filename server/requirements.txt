fastapi==0.141.1
uvicorn[standard]==0.52.3
httpx==0.28.1
python-multipart==0.0.32
python-dotenv==1.2.2

# Test-only (kept here for simplicity; split into requirements-dev.txt if the
# deploy image size ever matters).
pytest==9.1.1
