; Pytest config lives here rather than in a pyproject.toml: a pyproject with no
; [project] dependencies makes `fly launch`'s source scanner think this is a
; broken Python app and abort.
[pytest]
pythonpath = .
testpaths = tests
